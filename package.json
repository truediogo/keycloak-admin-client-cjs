{
  "name": "@truediogo/keycloak-admin-client-cjs",
  "version": "26.0.5",
  "description": "Keycloak Admin Client as CJS",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.ts --platform=node --bundle --format=cjs --outfile=index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@keycloak/keycloak-admin-client": "^26.0.5"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
